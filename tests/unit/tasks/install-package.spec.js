
const installPackage = require('../../../lib/tasks/install-package');

it('should spawn a thread to run npm install');
it('should gracefully handle a non-zero close');
it('should install an npm package');